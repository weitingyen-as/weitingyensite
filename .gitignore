# Hugo build output and caches — regenerated by CI on every push.
/public/
/resources/_gen/
/.hugo_build.lock
hugo_stats.json

# Node dependencies (package-lock.json IS committed).
/node_modules/

.DS_Store
